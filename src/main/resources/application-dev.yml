spring:
  datasource:
    url: jdbc:mysql://localhost:3306/adrovis_dev?useSSL=false&serverTimezone=UTC
    username: root
    password: yadav1

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.adrovis: DEBUG
    org.hibernate.SQL: DEBUG

cors:
  allowed-origins: http://localhost:3000