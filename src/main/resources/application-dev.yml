spring:
  datasource:
    url: jdbc:mysql://localhost:3306/adrovis_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: yadav1


  mail:
    host: smtp.zoho.in
    port: 587
    username: aditya.yadav@adrovis.com
    password: ZveJXvfuMuz3

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  app:
    mail:
      from: hello@adrovis.com


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.adrovis: DEBUG
    org.hibernate.SQL: DEBUG

app:
  cors:
    allowed-origins:
      - http://localhost:3000

  async:
    core-pool-size: 2
    max-pool-size: 5
    queue-capacity: 100
    await-termination-seconds: 30

  mail:
    from: aditya.yadav@adrovis.com

  api:
    base-url: http://localhost:8080
    environment-label: Development


springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true