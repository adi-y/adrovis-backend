spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/adrovis_prod?useSSL=true&serverTimezone=UTC
    username: root
    password: yadav1

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.adrovis: WARN

app:
  cors:
    allowed-origins:
      - https://adrovis.com
      - https://www.adrovis.com

  async:
    core-pool-size: 4
    max-pool-size: 10
    queue-capacity: 200
    await-termination-seconds: 30

  api:
    base-url: https://api.adrovis.com
    environment-label: Production


springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false