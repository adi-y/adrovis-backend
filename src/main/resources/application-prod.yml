spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/adrovis_prod?useSSL=true&serverTimezone=UTC
    username: root
    password: yadav1

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.adrovis: WARN

cors:
  allowed-origins: ${FRONTEND_URL}